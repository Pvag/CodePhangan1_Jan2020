// Create a sketch that shows a black canvas;
// when the user presses (keeps pressed) the mouse button,
// the canvas becomes white; when the mouse button
// is released the canvas goes black again.

void setup() {
  size(400, 150);
}

void draw() {
  // little hint
  if (mousePressed) {
    // something happens here...
  }
  else {
    // ...something else here
  }  
}
