// Create a sketch that draws a "snake" of white circles
// with default border only when the mouse is dragged around
// (which means that the user is moving the mouse around
//  the canvas, while pressing its button).

void setup() {
  size(400, 180);
  background(0);
}

void draw() {
  // nothing here for this sketch
}

void mouseDragged() {
  // but something goes in here...
}
