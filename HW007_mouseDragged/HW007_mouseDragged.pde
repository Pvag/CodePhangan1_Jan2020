// Create a sketch that draws a "snake" of white circles,
// with default border, performing the drawing only when
// the mouse is dragged around
// (that is only when the user is moving the mouse around
//  the canvas, with its button pressed - that's a "mouse drag").

void setup() {
  size(400, 180);
  background(0);
}

void draw() {
  // nothing here for this sketch
}

void mouseDragged() {
  // but something goes in here...
}
